pre-commit:
  commands:
    check:
      glob: "*.{ts,tsx,js,jsx,json,jsonc,css}"
      run: ./node_modules/.bin/biome check --write --files-ignore-unknown=true --no-errors-on-unmatched {staged_files}
      stage_fixed: true

pre-push:
  commands:
    check:
      run: bun run check
